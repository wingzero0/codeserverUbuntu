version: '3.7'

services:
  compiler:
    image: wingzzz2003/codeserver_ubuntu:beta
    volumes:
      - ${PWD}/4.4.0/.config/code-server/config.yaml:/root/.config/code-server/config.yaml
    ports:
      - 8080:8080
    entrypoint: code-server
